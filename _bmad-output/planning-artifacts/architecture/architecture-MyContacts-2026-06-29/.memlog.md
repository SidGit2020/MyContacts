---
scope: MyContacts Birthday Reminder v1 feature
purpose: build-substrate — fix invariants for birthday reminder implementation within existing ASP.NET Core MVC app
altitude: feature
updated: 2026-06-29T12:53
---

- (constraint) Existing app: ASP.NET Core MVC, controllers access AppDbContext directly (no repository/service layer), Bootstrap layout, SQLite via EF Core
- (constraint) Existing Program.cs uses db.Database.EnsureCreated() — NOT migrations; must resolve before adding DateOfBirth column to existing DB
- (constraint) Existing Index view uses @model IEnumerable<Contact> — no ViewModel pattern currently in use
- (decision) Schema management: EnsureCreated retained for now — no existing data, DB can be recreated when schema changes. Prevents: running migrations tooling on a schema-less baseline. Note: Story 1.1 epics called for dotnet ef migrations add; will need to align story or move to Migrate() when real data accumulates.
- (decision) Birthday logic location: private method in ContactsController — GetUpcomingBirthdays(). Prevents: birthday calculation logic leaking into view or being duplicated. No service layer introduced.
- (decision) View data passing: ViewBag.UpcomingBirthdays carries List<(Contact, int daysUntil)> from Index action to view. Prevents: need for a new ViewModel class. Index view @model stays IEnumerable<Contact>.
- (decision) No abstraction layer (repository/service): controllers access AppDbContext directly. Ratified from existing code. Prevents: inconsistent access patterns across the app.
- (decision) Year-boundary birthday calc: create DateTime for birthday this calendar year; if already past, use next year. Diff against DateTime.Today. Prevents: wrong days-until for Dec-Jan window.
- (event) ARCHITECTURE-SPINE.md drafted — 6 ADs, full capability map, stack verified against csproj and wwwroot
- (event) contacts.db deleted — EnsureCreated will recreate it with updated schema (including DateOfBirth) on next app start
- (event) spine finalized — status set to final; Story 1.1 AC updated to delete-and-recreate approach
